import {
  GAME_CORE_UTILS_POLICY,
  GAME_CORE_UTILS_FUNCTION_NAMES,
} from "../deps/gameCoreUtilsDefinitions.js";

export const GAME_CORE_UTILS_CONTRACT = Object.freeze({
  policy: GAME_CORE_UTILS_POLICY,
  functionNames: GAME_CORE_UTILS_FUNCTION_NAMES,
});
